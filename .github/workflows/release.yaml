name: Release Artifacts

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag (e.g., v0.1.3)"
        required: true

jobs:
  build-artifacts:
    name: Build and Sign Artifacts
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${GITHUB_REF#refs/tags/}"
          fi
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Version: ${VERSION}"

      - name: Build artifact
        run: |
          chmod +x ci/build.sh
          ./ci/build.sh --build-artifact --artifact-version "${{ steps.version.outputs.version }}" --artifact-output ./artifacts

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: artifacts
          path: ./artifacts/*
          retention-days: 30

  create-release:
    name: Create Release and Upload Assets
    needs: build-artifacts
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: github.event_name != 'workflow_dispatch' || github.event.inputs.create_release == 'true'

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./release-artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.build-artifacts.outputs.version }}
          name: Release ${{ needs.build-artifacts.outputs.version }}
          files: ./release-artifacts/**/*
          draft: false
          prerelease: ${{ contains(needs.build-artifacts.outputs.version, '-rc') || contains(needs.build-artifacts.outputs.version, '-alpha') || contains(needs.build-artifacts.outputs.version, '-beta') }}
